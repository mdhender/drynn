// Package middleware holds HTTP middleware shared across handler groups.
//
// Populated in Phase 5 with structured slog request logging, token-bucket
// rate limiting, and Fetch Metadata enforcement.
package middleware
